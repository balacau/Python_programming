# Python_programming
Just playing around


Started og by writing the program in Python and getting AI to convert it to have an GUI.

The converted it to an executable with following command:

pyinstaller --onefile --windowed ohms_law_calculator.py
